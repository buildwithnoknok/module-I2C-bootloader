#ifndef _FUNCONFIG_H
#define _FUNCONFIG_H

// CH32V003 — noknok stage-0 (frozen bootloader stage)
// Stage-0 never calls SystemInit, so no clock config applies. Defaults from
// ch32fun.h are used only for register definitions.

#endif
